Bigfish 内置插件目录（bundled-plugins）
=======================================

这里放随软件内置、无需联网即可安装的 DSH 插件。

安装方式：插件市场 → 内置离线标签（或安装时选择「内置」来源）。

目录结构：每个子目录 = 一个插件包（含 package.json，声明 dsh.bundle.patch / dsh.client）。
